# Database Connection Configuration
# =================================
# This file shows examples of how to configure connections to different database types.
# Copy this to .env and fill in your actual credentials.

# Oracle Database (Default: DWH)
# ------------------------------
DWH_TYPE=oracle
DWH_USERNAME=your_username
DWH_PASSWORD=your_password
DWH_DSN=hostname:port/service_name
# Example: DWH_DSN=localhost:1521/ORCL

# MySQL Database Example
# ----------------------
MYSQL_DEV_TYPE=mysql
MYSQL_DEV_USERNAME=your_username
MYSQL_DEV_PASSWORD=your_password
MYSQL_DEV_HOST=localhost
MYSQL_DEV_PORT=3306
MYSQL_DEV_DATABASE=your_database

# PostgreSQL Database Example
# ---------------------------
PG_DEV_TYPE=postgresql
PG_DEV_USERNAME=your_username
PG_DEV_PASSWORD=your_password
PG_DEV_HOST=localhost
PG_DEV_PORT=5432
PG_DEV_DATABASE=your_database

# SQL Server Database Example
# ---------------------------
MSSQL_DEV_TYPE=sqlserver
MSSQL_DEV_USERNAME=your_username
MSSQL_DEV_PASSWORD=your_password
MSSQL_DEV_HOST=localhost
MSSQL_DEV_PORT=1433
MSSQL_DEV_DATABASE=your_database
# Optional: Specify ODBC driver (default: {ODBC Driver 17 for SQL Server})
# MSSQL_DEV_DRIVER={ODBC Driver 18 for SQL Server}
# Optional: Fix "Protocol error in TDS stream" (encryption mismatch)
# MSSQL_DEV_ENCRYPT=no
# MSSQL_DEV_TRUST_SERVER_CERTIFICATE=yes

# Notes:
# ------
# - The {ALIAS} prefix can be any name you choose (e.g., DWH, PROD, DEV, STAGING)
# - Use the same alias when calling scripts: --db {ALIAS}
# - Oracle uses DSN format: hostname:port/service_name
# - MySQL, PostgreSQL, SQL Server use HOST + PORT + DATABASE
# - SQL Server requires pyodbc package: pip install pyodbc
# - Oracle requires oracledb package: pip install oracledb
# - MySQL requires mysql-connector-python: pip install mysql-connector-python
# - PostgreSQL requires psycopg2: pip install psycopg2-binary

# SQL Server ODBC Driver Installation:
# ------------------------------------
# Windows: Download from Microsoft website
# Linux: 
#   - Ubuntu/Debian: apt-get install unixodbc-dev
#   - RedHat/CentOS: yum install unixODBC-devel
# macOS: brew install unixodbc
# Then install Microsoft ODBC Driver: https://learn.microsoft.com/en-us/sql/connect/odbc/download-odbc-driver-for-sql-server
